<mujoco model="cartpole">
    <compiler angle="radian"/>

    <option integrator="implicitfast"
            timestep="0.01"
            gravity="0 0 -9.81"/>

    <asset>
        <texture type="skybox" builtin="gradient"
                 rgb1="0.6 0.7 0.9" rgb2="0.3 0.3 0.4"
                 width="512" height="3072"/>
        <texture type="2d" name="groundplane" builtin="checker"
                 mark="edge" rgb1="0.2 0.3 0.4"
                 rgb2="0.1 0.2 0.3"
                 markrgb="0.8 0.8 0.8"
                 width="300" height="300"/>
        <material name="groundplane"
                  texture="groundplane"
                  texuniform="true"
                  texrepeat="5 5"
                  reflectance="0.2"/>
    </asset>

    <worldbody>
        <!-- Lights -->
        <light name="global_light" directional="true"
               diffuse="1 1 1" specular="0.3 0.3 0.3"
               castshadow="false"/>
        <light pos="-5 0 10"/>
        <light pos="0 0 10"/>
        <light pos="5 0 10"/>

        <!-- Floor -->
        <geom name="floor" type="plane"
              pos="0 0 0"
              size="0 0 0.05"
              material="groundplane"/>

        <camera name="fixed" pos="0 -4 1.5" xyaxes="1 0 0 0 0 1"/>

        <!-- Cart -->
        <body name="cart" pos="0 0 1.1">
            <!-- <joint name="cart_slide"
                   type="slide"
                   axis="1 0 0"
                   damping="1"/> -->

            <joint name="cart_slide"
                type="slide"
                axis="1 0 0"
                stiffness="5"
                damping="1"/>

            <geom name="cart_geom"
                  type="box"
                  size="0.2 0.1 0.05"
                  rgba="0.2 0.2 0.2 1"
                  mass="1"/>

            <!-- Pole -->
            <body name="pole" pos="0 0 0.05">
                <joint name="pole_hinge"
                       type="hinge"
                       axis="0 1 0"
                       damping="0.05"/>

                <geom name="pole_geom"
                      type="capsule"
                      fromto="0 0 0 0 0 -1"
                      size="0.02"
                      mass="0.5"
                      rgba="1 0 0 1"/>
            </body>
        </body>
    </worldbody>

    <actuator>
        <!-- Control force applied to cart -->
        <motor joint="cart_slide"
               ctrlrange="-1 1"
               gear="5"/>
    </actuator>
</mujoco>